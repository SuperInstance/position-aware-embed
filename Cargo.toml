[package]
repository = "https://github.com/SuperInstance/position-aware-embed"
name = "position-aware-embed"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Position-aware text embedding — 44% top-1 accuracy for command matching, sub-microsecond latency"

[dependencies]
blake2 = "0.10"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "embed_bench"
harness = false
